<section id="cheat-chaos" class="chaos-cheat" aria-label="Chaos mode" aria-hidden="true">
	<div class="chaos-cheat-head">
		<p class="chaos-cheat-title">Chaos Physics Mode</p>
		<span id="chaos-cheat-status" class="chaos-cheat-status">Disabled</span>
	</div>
	<p class="chaos-cheat-note">Disable gravity and drag site elements around. Reset to restore the page instantly.</p>
	<div class="chaos-cheat-controls">
		<label class="chaos-toggle" for="chaos-toggle">
			<input id="chaos-toggle" type="checkbox" />
			<span class="chaos-toggle-track" aria-hidden="true"></span>
			<span class="chaos-toggle-label">Disable gravity</span>
		</label>
		<button id="chaos-reset" type="button" class="chaos-cheat-button">Reset Layout</button>
	</div>
</section>

<style>
	.chaos-cheat {
		display: none;
		position: relative;
		z-index: 1;
		margin-top: 0.85rem;
		padding: 0.75rem;
		border-radius: 14px;
		border: 1px solid rgba(255, 255, 255, 0.18);
		background: linear-gradient(180deg, rgba(8, 8, 16, 0.9), rgba(19, 8, 26, 0.84));
	}

	.chaos-cheat.is-open {
		display: grid;
		gap: 0.65rem;
	}

	.chaos-cheat-head {
		display: flex;
		justify-content: space-between;
		align-items: center;
		gap: 0.5rem;
	}

	.chaos-cheat-title {
		margin: 0;
		font-family: 'JetBrains Mono', monospace;
		font-size: 0.66rem;
		text-transform: uppercase;
		letter-spacing: 0.12em;
		color: rgba(205, 175, 255, 0.95);
	}

	.chaos-cheat-status {
		font-family: 'JetBrains Mono', monospace;
		font-size: 0.62rem;
		text-transform: uppercase;
		letter-spacing: 0.08em;
		padding: 0.25rem 0.42rem;
		border-radius: 999px;
		border: 1px solid rgba(255, 255, 255, 0.24);
		background: rgba(255, 255, 255, 0.06);
	}

	.chaos-cheat-status.is-active {
		border-color: rgba(192, 132, 252, 0.8);
		background: rgba(168, 85, 247, 0.28);
		color: rgba(244, 230, 255, 0.98);
	}

	.chaos-cheat-note {
		margin: 0;
		font-size: 0.78rem;
		line-height: 1.45;
		color: rgba(255, 255, 255, 0.78);
	}

	.chaos-cheat-controls {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 0.6rem;
		flex-wrap: wrap;
	}

	.chaos-toggle {
		display: grid;
		grid-template-columns: auto 1fr;
		align-items: center;
		gap: 0.52rem;
	}

	.chaos-toggle input {
		position: absolute;
		opacity: 0;
		pointer-events: none;
	}

	.chaos-toggle-track {
		position: relative;
		display: inline-block;
		width: 38px;
		height: 22px;
		border-radius: 999px;
		border: 1px solid rgba(255, 255, 255, 0.3);
		background: rgba(255, 255, 255, 0.14);
		transition: background 0.2s ease, border-color 0.2s ease;
	}

	.chaos-toggle-track::after {
		content: '';
		position: absolute;
		top: 2px;
		left: 2px;
		width: 16px;
		height: 16px;
		border-radius: 50%;
		background: #fff;
		transition: transform 0.2s ease;
	}

	.chaos-toggle input:checked + .chaos-toggle-track {
		background: rgba(168, 85, 247, 0.35);
		border-color: rgba(216, 180, 255, 0.9);
	}

	.chaos-toggle input:checked + .chaos-toggle-track::after {
		transform: translateX(16px);
	}

	.chaos-toggle-label {
		font-size: 0.76rem;
		letter-spacing: 0.03em;
	}

	.chaos-cheat-button {
		appearance: none;
		border: 1px solid rgba(255, 255, 255, 0.26);
		border-radius: 8px;
		padding: 0.4rem 0.56rem;
		font-family: 'JetBrains Mono', monospace;
		font-size: 0.62rem;
		text-transform: uppercase;
		letter-spacing: 0.08em;
		background: rgba(255, 255, 255, 0.08);
		color: #fff;
		transition: background 0.2s ease, border-color 0.2s ease;
	}

	.chaos-cheat-button:hover,
	.chaos-cheat-button:focus-visible {
		outline: none;
		border-color: rgba(216, 180, 255, 0.9);
		background: rgba(168, 85, 247, 0.26);
	}
</style>
