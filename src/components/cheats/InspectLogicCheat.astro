<section id="cheat-inspect-logic" class="inspect-logic-cheat" aria-label="Inspect logic" aria-hidden="true">
	<div class="inspect-logic-head">
		<p class="inspect-logic-title">Inspect Logic</p>
		<span id="inspect-logic-status" class="inspect-logic-status">Off</span>
	</div>
	<p class="inspect-logic-note">Draws flow lines from skill nodes to matching projects to visualize architecture and usage.</p>
	<div class="inspect-logic-controls">
		<label class="inspect-logic-toggle" for="inspect-logic-toggle">
			<input id="inspect-logic-toggle" type="checkbox" />
			<span class="inspect-logic-track" aria-hidden="true"></span>
			<span class="inspect-logic-label">Show connection graph</span>
		</label>
		<button id="inspect-logic-refresh" type="button" class="inspect-logic-button">Refresh</button>
	</div>
</section>

<style>
	.inspect-logic-cheat {
		display: none;
		position: relative;
		z-index: 1;
		margin-top: 0.85rem;
		padding: 0.75rem;
		border-radius: 14px;
		border: 1px solid rgba(255, 255, 255, 0.18);
		background: linear-gradient(180deg, rgba(6, 14, 24, 0.9), rgba(8, 20, 14, 0.86));
	}

	.inspect-logic-cheat.is-open {
		display: grid;
		gap: 0.65rem;
	}

	.inspect-logic-head {
		display: flex;
		justify-content: space-between;
		align-items: center;
		gap: 0.5rem;
	}

	.inspect-logic-title {
		margin: 0;
		font-family: 'JetBrains Mono', monospace;
		font-size: 0.66rem;
		text-transform: uppercase;
		letter-spacing: 0.12em;
		color: rgba(145, 242, 214, 0.95);
	}

	.inspect-logic-status {
		font-family: 'JetBrains Mono', monospace;
		font-size: 0.62rem;
		text-transform: uppercase;
		letter-spacing: 0.08em;
		padding: 0.25rem 0.42rem;
		border-radius: 999px;
		border: 1px solid rgba(255, 255, 255, 0.24);
		background: rgba(255, 255, 255, 0.06);
	}

	.inspect-logic-status.is-active {
		border-color: rgba(74, 222, 128, 0.85);
		background: rgba(34, 197, 94, 0.26);
		color: rgba(222, 255, 237, 0.98);
	}

	.inspect-logic-note {
		margin: 0;
		font-size: 0.78rem;
		line-height: 1.45;
		color: rgba(255, 255, 255, 0.78);
	}

	.inspect-logic-controls {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 0.6rem;
		flex-wrap: wrap;
	}

	.inspect-logic-toggle {
		display: grid;
		grid-template-columns: auto 1fr;
		align-items: center;
		gap: 0.52rem;
	}

	.inspect-logic-toggle input {
		position: absolute;
		opacity: 0;
		pointer-events: none;
	}

	.inspect-logic-track {
		position: relative;
		display: inline-block;
		width: 38px;
		height: 22px;
		border-radius: 999px;
		border: 1px solid rgba(255, 255, 255, 0.3);
		background: rgba(255, 255, 255, 0.14);
		transition: background 0.2s ease, border-color 0.2s ease;
	}

	.inspect-logic-track::after {
		content: '';
		position: absolute;
		top: 2px;
		left: 2px;
		width: 16px;
		height: 16px;
		border-radius: 50%;
		background: #fff;
		transition: transform 0.2s ease;
	}

	.inspect-logic-toggle input:checked + .inspect-logic-track {
		background: rgba(16, 185, 129, 0.35);
		border-color: rgba(110, 231, 183, 0.92);
	}

	.inspect-logic-toggle input:checked + .inspect-logic-track::after {
		transform: translateX(16px);
	}

	.inspect-logic-label {
		font-size: 0.76rem;
		letter-spacing: 0.03em;
	}

	.inspect-logic-button {
		appearance: none;
		border: 1px solid rgba(255, 255, 255, 0.26);
		border-radius: 8px;
		padding: 0.4rem 0.56rem;
		font-family: 'JetBrains Mono', monospace;
		font-size: 0.62rem;
		text-transform: uppercase;
		letter-spacing: 0.08em;
		background: rgba(255, 255, 255, 0.08);
		color: #fff;
		transition: background 0.2s ease, border-color 0.2s ease;
	}

	.inspect-logic-button:hover,
	.inspect-logic-button:focus-visible {
		outline: none;
		border-color: rgba(110, 231, 183, 0.92);
		background: rgba(16, 185, 129, 0.24);
	}
</style>
