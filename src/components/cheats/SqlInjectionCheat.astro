<section id="cheat-sql-sim" class="sql-cheat" aria-label="SQL Injection simulator" aria-hidden="true">
	<div class="sql-cheat-head">
		<p class="sql-cheat-title">SQL Injection Simulator</p>
		<span id="sql-cheat-status" class="sql-cheat-status">Locked</span>
	</div>
	<p class="sql-cheat-note">Type the query to hack the safe database simulation and reveal hidden projects.</p>
	<div id="sql-cheat-output" class="sql-cheat-output" role="log" aria-live="polite">
		<p class="sql-cheat-line">db://portfolio.secure</p>
		<p class="sql-cheat-line">Hint: <span class="sql-hint">SELECT * FROM secret_projects;</span></p>
	</div>
	<form id="sql-cheat-form" class="sql-cheat-input-row">
		<label for="sql-cheat-input" class="sql-sr-only">SQL command input</label>
		<span class="sql-prompt">sql&gt;</span>
		<input id="sql-cheat-input" class="sql-cheat-input" name="sql" autocomplete="off" spellcheck="false" placeholder="SELECT * FROM ..." />
		<button type="submit" class="sql-cheat-button">Execute</button>
	</form>
</section>

<style>
	.sql-cheat {
		display: none;
		position: relative;
		z-index: 1;
		margin-top: 0.85rem;
		padding: 0.75rem;
		border-radius: 14px;
		border: 1px solid rgba(255, 255, 255, 0.2);
		background: linear-gradient(180deg, rgba(8, 13, 20, 0.95), rgba(15, 10, 16, 0.92));
	}

	.sql-cheat.is-open {
		display: grid;
		gap: 0.65rem;
	}

	.sql-cheat-head {
		display: flex;
		justify-content: space-between;
		align-items: center;
		gap: 0.5rem;
	}

	.sql-cheat-title {
		margin: 0;
		font-family: 'JetBrains Mono', monospace;
		font-size: 0.66rem;
		text-transform: uppercase;
		letter-spacing: 0.12em;
		color: rgba(255, 195, 195, 0.95);
	}

	.sql-cheat-status {
		font-family: 'JetBrains Mono', monospace;
		font-size: 0.62rem;
		text-transform: uppercase;
		letter-spacing: 0.08em;
		padding: 0.25rem 0.42rem;
		border-radius: 999px;
		border: 1px solid rgba(255, 255, 255, 0.24);
		background: rgba(255, 255, 255, 0.06);
	}

	.sql-cheat-status.is-open {
		border-color: rgba(34, 197, 94, 0.85);
		background: rgba(22, 163, 74, 0.24);
		color: rgba(227, 255, 234, 0.98);
	}

	.sql-cheat-note {
		margin: 0;
		font-size: 0.78rem;
		line-height: 1.45;
		color: rgba(255, 255, 255, 0.78);
	}

	.sql-cheat-output {
		max-height: 150px;
		overflow: auto;
		border-radius: 10px;
		padding: 0.5rem 0.55rem;
		border: 1px solid rgba(255, 255, 255, 0.16);
		background: rgba(0, 0, 0, 0.4);
		font-family: 'JetBrains Mono', monospace;
		font-size: 0.7rem;
		line-height: 1.45;
		color: rgba(238, 246, 255, 0.95);
	}

	.sql-cheat-line {
		margin: 0;
		white-space: pre-wrap;
		word-break: break-word;
	}

	.sql-cheat-line + .sql-cheat-line {
		margin-top: 0.2rem;
	}

	.sql-cheat-line.is-error {
		color: rgba(255, 172, 172, 0.96);
	}

	.sql-cheat-line.is-success {
		color: rgba(162, 255, 200, 0.95);
	}

	.sql-hint {
		color: rgba(255, 210, 140, 0.95);
	}

	.sql-cheat-input-row {
		display: grid;
		grid-template-columns: auto 1fr auto;
		align-items: center;
		gap: 0.5rem;
	}

	.sql-prompt {
		font-family: 'JetBrains Mono', monospace;
		font-size: 0.74rem;
		color: rgba(255, 198, 164, 0.95);
	}

	.sql-cheat-input {
		width: 100%;
		border-radius: 8px;
		border: 1px solid rgba(255, 255, 255, 0.2);
		background: rgba(255, 255, 255, 0.06);
		color: #fff;
		padding: 0.42rem 0.5rem;
		font-family: 'JetBrains Mono', monospace;
		font-size: 0.72rem;
	}

	.sql-cheat-input:focus-visible {
		outline: none;
		border-color: rgba(255, 190, 120, 0.82);
	}

	.sql-cheat-button {
		appearance: none;
		border: 1px solid rgba(255, 255, 255, 0.24);
		border-radius: 8px;
		padding: 0.38rem 0.52rem;
		font-family: 'JetBrains Mono', monospace;
		font-size: 0.62rem;
		text-transform: uppercase;
		letter-spacing: 0.08em;
		background: rgba(255, 255, 255, 0.08);
		color: #fff;
		transition: background 0.2s ease, border-color 0.2s ease;
	}

	.sql-cheat-button:hover,
	.sql-cheat-button:focus-visible {
		outline: none;
		border-color: rgba(255, 203, 157, 0.9);
		background: rgba(255, 161, 79, 0.2);
	}

	.sql-sr-only {
		position: absolute;
		width: 1px;
		height: 1px;
		padding: 0;
		margin: -1px;
		overflow: hidden;
		clip: rect(0, 0, 0, 0);
		border: 0;
	}
</style>
